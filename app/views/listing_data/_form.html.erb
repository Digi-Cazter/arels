<%= form_for(@listing_datum) do |f| %>
  <% if @listing_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing_datum.errors.count, "error") %> prohibited this listing_datum from being saved:</h2>

      <ul>
      <% @listing_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
